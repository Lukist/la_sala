<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.La_sala_project"
        tools:targetApi="31"
        >
        <receiver android:name=".Alarma.AlarmReceiver"/>

        <activity
            android:name=".actividades.AlumnoInfo"
            android:exported="false" />
        <activity
            android:name=".actividades.Clases"
            android:exported="false" />
        <activity
            android:name=".actividades.Alumnos"
            android:exported="false" />
        <activity
            android:name=".actividades.Deudas"
            android:exported="false" />
        <activity
            android:name=".actividades.Login"
            android:exported="false">

        </activity>
        <activity
            android:name=".actividades.MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>